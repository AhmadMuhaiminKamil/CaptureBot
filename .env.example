# ── WAJIB ──────────────────────────────
BOT_TOKEN=           # Token bot Telegram dari @BotFather

# ── Supabase (wajib untuk simpan data capture) ──
SUPABASE_URL=        # URL project Supabase
SUPABASE_KEY=        # Service role key atau anon key

# ── Vercel (hanya untuk mode webhook) ──
VERCEL_URL=          # Contoh: https://project.vercel.app

# ── Opsional ───────────────────────────
# PORT=8080          # Port untuk health check di mode polling
